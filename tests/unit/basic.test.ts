import {describe, expect, test} from 'vitest'

describe('Composables/Utils testing', async () => {

    test('Are both values are same', async () => {
        const x = 1
        const y = 1
        expect(x === y).toBe(true)
    })


})
